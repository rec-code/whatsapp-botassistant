from core.bot_modules_core import BotModulesCore 


class BotBase(BotModulesCore):
    def __init__(self, name):
        super(BotBase, self).__init__(name)

    def base(self, bot):
        if not self.enabled:
            return